<Project Sdk="Microsoft.NET.Sdk">

  <!-- Phase 3 builds this out. Stub only in Phase 1. UseWindowsForms is added
       in Phase 3 for the first-party NotifyIcon (ADR-0005). -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <RootNamespace>OView.Tray</RootNamespace>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\O-view.Core\O-view.Core.csproj" />
  </ItemGroup>

</Project>
