<Window x:Class="OView.Tray.Popup.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OView.Tray.Popup"
        Width="400" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" AllowsTransparency="True" Background="Transparent"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <ResourceDictionary>
            <!-- The shared hover card, so the graph's bars and gridlines carry the same
                 branding as the tiles instead of falling back to system chrome. -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="HoverCard.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- A text disclosure. A Button rather than a clickable TextBlock for the same
                 reason StatTile is one: it keeps keyboard focus and Space/Enter activation,
                 which a MouseLeftButtonUp handler silently does not. -->
            <Style x:Key="DisclosureButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0,1"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="3" Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="Content" Opacity="0.75"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Content" Property="Opacity" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Content" Property="Opacity" Value="1"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- One segment of the Bars/Breakdown switch (issue #253). Templated off
                 RadioButton so the group keeps exclusivity, arrow-key traversal and the
                 correct accessibility role; the bullet is replaced, not hidden, because a
                 collapsed bullet still reserves its width and shifts the label. -->
            <Style x:Key="ViewSwitchButton" TargetType="RadioButton">
                <Setter Property="FontSize" Value="10.5"/>
                <Setter Property="Padding" Value="8,3"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="3"
                                    Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                <TextBlock Text="{TemplateBinding Content}"
                                           FontSize="{TemplateBinding FontSize}"
                                           Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background"
                                            Value="{DynamicResource TileHover}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                </Trigger>
                                <!-- Checked wins over hover: it is state, not feedback, and a
                                     hover that repaints the selected segment reads as though
                                     the selection moved with the pointer. -->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background"
                                            Value="{DynamicResource BarTrack}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="Background"
                                            Value="{DynamicResource TilePressed}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border x:Name="PanelRoot" Background="{DynamicResource PanelBg}" BorderBrush="{DynamicResource PanelBorder}"
            BorderThickness="1" CornerRadius="10" Padding="16">
        <StackPanel>

            <!-- Header: title + freshness left, account right (ui-spec.md) -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="O-view" FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock x:Name="UpdatedText" FontSize="11" Margin="0,2,0,0"
                               Foreground="{DynamicResource TextMuted}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock x:Name="NameText" FontSize="12" FontWeight="SemiBold" TextAlignment="Right"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock x:Name="EmailText" FontSize="11" TextAlignment="Right"
                               Foreground="{DynamicResource TextSecondary}"/>
                    <Border x:Name="TierBadge" Background="{DynamicResource BadgeBg}" CornerRadius="4"
                            Padding="6,1" Margin="0,3,0,0" HorizontalAlignment="Right">
                        <TextBlock x:Name="TierText" FontSize="10" Foreground="{DynamicResource BadgeText}"/>
                    </Border>
                </StackPanel>
            </Grid>

            <Separator x:Name="HeaderSeparator" Margin="0,12,0,12" Background="{DynamicResource PanelBorder}"/>

            <!-- Off-plan warning: the plan % is true but not the whole story -->
            <Border x:Name="DivergenceBanner" Background="{DynamicResource WarnBg}" CornerRadius="6"
                    Padding="10,8" Margin="0,0,0,12" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock x:Name="DivergenceTitle" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource WarnText}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="DivergenceDetail" FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"
                               Foreground="{DynamicResource TextSecondary}"/>
                </StackPanel>
            </Border>

            <!-- Why the figures read "unknown". A blank panel with no explanation cost
                 four rounds of support to diagnose; the app should state the reason. -->
            <Border x:Name="NoDataBanner" Background="{DynamicResource WarnBg}" CornerRadius="6"
                    Padding="10,8" Margin="0,0,0,12" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock x:Name="NoDataTitle" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource WarnText}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="NoDataDetail" FontSize="11" TextWrapping="Wrap" Margin="0,3,0,0"
                               Foreground="{DynamicResource TextSecondary}"/>
                </StackPanel>
            </Border>

            <!-- Session bar -->
            <Grid>
                <TextBlock Text="Current session" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock x:Name="SessionPctText" FontSize="12" HorizontalAlignment="Right"
                           Foreground="{DynamicResource TextSecondary}"/>
            </Grid>
            <Grid x:Name="SessionBar" Height="8" Margin="0,6,0,4">
                <Border Background="{DynamicResource BarTrack}" CornerRadius="4"/>
                <Border x:Name="SessionBarFill" CornerRadius="4" HorizontalAlignment="Left"/>
            </Grid>
            <TextBlock x:Name="SessionResetText" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>

            <!-- Weekly bar -->
            <!-- Three columns, not an overlay. The percentage is right-aligned in its own
                 Auto column so it reserves its width, and the chip sits in the star column
                 between them: the label and the figure both keep their space whatever the
                 chip says, and an over-long chip is trimmed rather than drawn over the
                 figure. Note the figure reads "51% used", not "51%" — a chip budget worked
                 out against the shorter string is about 30px too generous. -->
            <Grid x:Name="WeeklyHeading" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Weekly" FontSize="12" FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimary}"/>
                <Border x:Name="BoostChip" Grid.Column="1"
                        Background="{DynamicResource BadgeBg}" CornerRadius="4"
                        Padding="6,1" Margin="8,0,8,0" VerticalAlignment="Center"
                        HorizontalAlignment="Left" Visibility="Collapsed">
                    <TextBlock x:Name="BoostChipText" FontSize="10"
                               TextTrimming="CharacterEllipsis"
                               Foreground="{DynamicResource BadgeText}"/>
                </Border>
                <TextBlock x:Name="WeeklyPctText" Grid.Column="2" FontSize="12"
                           VerticalAlignment="Center" HorizontalAlignment="Right"
                           Foreground="{DynamicResource TextSecondary}"/>
            </Grid>
            <Grid x:Name="WeeklyBar" Height="8" Margin="0,6,0,4">
                <Border Background="{DynamicResource BarTrack}" CornerRadius="4"/>
                <Border x:Name="WeeklyBarFill" CornerRadius="4" HorizontalAlignment="Left"/>
            </Grid>
            <TextBlock x:Name="WeeklyResetText" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>

            <!-- Stats tiles 2×2. Each flips to a per-model breakdown on click (issue
                 #37) — see StatTile; the four were four copies of the same markup, so
                 they are one control now rather than four places to keep in step. -->
            <UniformGrid x:Name="TileGrid" Columns="2" Margin="0,14,0,0">
                <local:StatTile x:Name="TileTokensToday" Margin="0,0,4,4"/>
                <local:StatTile x:Name="TileEstToday" Margin="4,0,0,4"/>
                <local:StatTile x:Name="TileTokens31" Margin="0,4,4,0"/>
                <local:StatTile x:Name="TileEst31" Margin="4,4,0,0"/>
            </UniformGrid>

            <!-- What none of the four tiles above cover (issue #235). The plan bars are
                 account-wide; these are not, because cloud-container Cowork sessions and
                 chat leave no transcript on this machine. Beneath the whole block rather
                 than on a tile: it is true of all four, and the per-tile caveat channel
                 would print it twice on the 31-day pair and never on today's.

                 Not the line issue #232 removed — that one sat under the session BAR,
                 described the session window alone, and carried a disclosure. -->
            <TextBlock x:Name="TokenScopeLine" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"
                       Foreground="{DynamicResource TextMuted}"/>

            <!-- What was billed, beside the tiles that headline only output (issue #253).
                 ~89% of a Claude Code total is cached prompt re-reads, which is why the
                 tiles used to read in millions where Claude's own context figure reads in
                 thousands. Drawn to scale that leaves output at 4 px of a 370 px track, so
                 the bar's job is the composition and the tiles' job is the headline.

                 This replaces the one-line split and its "Why so large?" disclosure. The
                 disclosure answered a question the panel no longer prompts: nothing on
                 screen is unexpectedly large any more. Collapsed entirely when there is
                 nothing to break down — a composition of zero explains nothing. -->
            <StackPanel x:Name="TokensUsedSection" Margin="0,12,0,0" Visibility="Collapsed">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TokensUsedHeading" Grid.Column="0" FontSize="11"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondary}"/>

                    <!-- RadioButtons rather than ToggleButtons: the two views are mutually
                         exclusive, and the group gives arrow-key traversal and the correct
                         accessibility role without a line of code. -->
                    <Border Grid.Column="1" Background="{DynamicResource TileBg}" CornerRadius="4" Padding="2">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="TokenViewBars" GroupName="TokenView"
                                         Style="{StaticResource ViewSwitchButton}" IsChecked="True"/>
                            <RadioButton x:Name="TokenViewBreakdown" GroupName="TokenView"
                                         Style="{StaticResource ViewSwitchButton}" Margin="2,0,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Both views are built in code: the segment count varies with how many
                     kinds are present, and a kind with no tokens gets no segment at all
                     (TokenBarGeometry). Templating that in XAML would put the drop rule in
                     two places. -->
                <StackPanel x:Name="TokenBarsView" Margin="0,6,0,0"/>

                <!-- The breakdown FOLDS in beneath the bars rather than replacing them, which
                     is the one place this departs from the mockup and does so deliberately.
                     Switching views changes the panel's height, and a panel that fits one way
                     and not the other is clipped by the taskbar — the exact failure the
                     disclosure's re-dock path was built to prevent, and the reason the
                     fold-check harness exists. Reusing it is worth more than the pixels replacing the
                     bars would save, and keeping the shape on screen while reading the figures
                     is the better reading order anyway.

                     ClipToBounds is required, not cosmetic: the fold animates this panel's
                     Height, and without it the table is drawn at full size outside the height
                     it has been given — over the section below and past the panel's border. -->
                <StackPanel x:Name="TokenBreakdownView" Margin="0,6,0,0"
                            Visibility="Collapsed" ClipToBounds="True"/>

                <!-- Which surfaces these figures are made of (issue #171). It lives under the
                     breakdown rather than the bars: it is provenance, which is what a reader
                     switching to the detail view is asking about. -->
                <TextBlock x:Name="TokenCoverageLine" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"
                           Foreground="{DynamicResource TextMuted}" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Scope note. The bars above and the tiles here measure DIFFERENT things:
                 the plan meters cover all Claude usage, the token tiles only usage that
                 leaves a Claude Code transcript. A Desktop-only user therefore sees a
                 non-zero session % beside 0 tokens, and a bare "0" reads as "you used
                 nothing" rather than "this counts something else". -->
            <TextBlock x:Name="TokenScopeNote" FontSize="11" TextWrapping="Wrap" Margin="0,6,0,0"
                       Foreground="{DynamicResource TextSecondary}" Visibility="Collapsed"/>

            <!-- Usage graph: daily bars, calendar-week gridlines, blue-by-week intensity,
                 vertical date labels, hover tooltips (issues #4, #5) -->
            <TextBlock x:Name="GraphHeading" Text="Usage · last 31 days" FontSize="12" FontWeight="SemiBold" Margin="0,14,0,6"
                       Foreground="{DynamicResource TextPrimary}"/>
            <Border Background="{DynamicResource TileBg}" CornerRadius="6" Padding="8,6">
                <Canvas x:Name="GraphHost" Height="86" HorizontalAlignment="Stretch"/>
            </Border>

            <!-- Credits: deferred pending a data source (ui-spec.md) -->
            <Separator x:Name="CreditsSeparator" Margin="0,12,0,10" Background="{DynamicResource PanelBorder}"/>
            <!-- The whole block is the hover target (issue #181): the caveat used to sit
                 below as two or three permanent lines, which is a lot of standing text for
                 something most users read once. It moves to a hover card covering both the
                 heading and the spend figure — the two places a reader's eye actually lands
                 when asking "what is this number?".

                 Background="Transparent" is load-bearing. A panel with no background has no
                 hit-testing in the gaps between its children, so the tooltip would appear
                 over the text and nowhere else — which is exactly the fiddly behaviour the
                 issue is trying to get away from. -->
            <StackPanel x:Name="CreditsSection" Background="Transparent">
                <Grid>
                    <TextBlock Text="Off-plan usage · last 31 days" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <Border x:Name="CreditsBadge" Background="{DynamicResource WarnBg}" CornerRadius="4"
                            Padding="6,1" HorizontalAlignment="Right">
                        <TextBlock x:Name="CreditsBadgeText" FontSize="10" Foreground="{DynamicResource WarnText}"/>
                    </Border>
                </Grid>
                <Grid Margin="0,6,0,0">
                    <TextBlock x:Name="CreditsSpendLabel" FontSize="11" Foreground="{DynamicResource TextSecondary}"/>
                    <TextBlock x:Name="CreditsSpendValue" FontSize="17" FontWeight="SemiBold"
                               HorizontalAlignment="Right" Foreground="{DynamicResource TextPrimary}"/>
                </Grid>
                <!-- The coverage caveat STAYS on screen. It qualifies the figure directly
                     above it — "30 of 31 days recorded" is the difference between a real
                     total and a partial one — and rule 6 makes that a requirement rather
                     than a nicety. Only the explanatory prose moves to hover. -->
                <TextBlock x:Name="CreditsCoverage" FontSize="10" Margin="0,2,0,0"
                           Foreground="{DynamicResource WarnText}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
