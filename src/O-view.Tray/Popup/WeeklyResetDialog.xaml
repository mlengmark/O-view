<Window x:Class="OView.Tray.Popup.WeeklyResetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="400" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Topmost="True" AllowsTransparency="True" Background="Transparent"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <!-- The same brand arc DialogWindow and the tray menu use, so this reads as the
             same application rather than a system box wearing its title. -->
        <LinearGradientBrush x:Key="BrandArc" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F4A17A" Offset="0"/>
            <GradientStop Color="#D9603A" Offset="1"/>
        </LinearGradientBrush>

        <!-- WPF's stock TextBox and ComboBox arrive with system chrome: white fill, a hard
             border, an Aero dropdown. On a dark themed panel that is not a small mismatch,
             it is the only thing on screen that looks like a different program. Both are
             re-templated onto the shared palette, which is also why the panel has no other
             input controls to copy from — these are the first. -->
        <Style x:Key="ThemedTextBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBg}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{DynamicResource TileBg}"
                                BorderBrush="{DynamicResource PanelBorder}" BorderThickness="1"
                                CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource TileHover}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemedComboItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}"
                                CornerRadius="4" SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource RowHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThemedComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ThemedComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd" Background="{DynamicResource TileBg}"
                                                BorderBrush="{DynamicResource PanelBorder}" BorderThickness="1"
                                                CornerRadius="5" SnapsToDevicePixels="True">
                                            <Grid>
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"
                                                                  Margin="9,0,26,0"/>
                                                <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Margin="0,0,10,0" Data="M0,0 L4,4 L8,0"
                                                      Stroke="{DynamicResource TextMuted}" StrokeThickness="1.3"
                                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource TileHover}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              IsHitTestVisible="False" Margin="9,0,26,0"
                                              VerticalAlignment="Center"/>

                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom"
                                   AllowsTransparency="True" PopupAnimation="None">
                                <Border Background="{DynamicResource PanelBg}"
                                        BorderBrush="{DynamicResource PanelBorder}" BorderThickness="1"
                                        CornerRadius="6" Padding="4" Margin="0,3,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <StackPanel IsItemsHost="True"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{DynamicResource AccentBg}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent"
                                BorderBrush="{DynamicResource PanelBorder}" BorderThickness="1"
                                CornerRadius="6" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource RowHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource PanelBg}" BorderBrush="{DynamicResource PanelBorder}"
            BorderThickness="1" CornerRadius="10" Padding="18">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Grid Width="16" Height="16" VerticalAlignment="Center">
                    <Ellipse Width="16" Height="16" Stroke="{StaticResource BrandArc}" StrokeThickness="2.5"/>
                    <Ellipse Width="5.5" Height="5.5" Fill="{StaticResource BrandArc}"
                             HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <TextBlock Text="Weekly reset time" FontSize="13" FontWeight="SemiBold" Margin="9,0,0,0"
                           VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
            </StackPanel>

            <!-- Names the source rather than describing the field. Anthropic assigns this
                 time per account and shows it in Claude; the user is copying a fact, not
                 estimating one, which is the whole reason it outranks what O-view derives. -->
            <TextBlock Text="Anthropic assigns each account a fixed weekly reset. Find yours in Claude → Settings → Usage."
                       FontSize="11" TextWrapping="Wrap" Margin="0,4,0,14"
                       Foreground="{DynamicResource TextSecondary}"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="96"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Day" FontSize="11" Margin="0,0,0,4"
                               Foreground="{DynamicResource TextMuted}"/>
                    <ComboBox x:Name="DayBox" Height="30" FontSize="13" Style="{StaticResource ThemedComboBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Time" FontSize="11" Margin="0,0,0,4"
                               Foreground="{DynamicResource TextMuted}"/>
                    <TextBox x:Name="TimeBox" Height="30" FontSize="13" Padding="9,0"
                             Style="{StaticResource ThemedTextBox}"/>
                </StackPanel>
            </Grid>

            <!-- Nothing is pre-filled from the derived value: a guess the user confirms
                 becomes a fact, which is worse than one labelled uncertain (issue #186). -->
            <TextBlock x:Name="HintText" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"
                       Foreground="{DynamicResource TextMuted}"/>

            <TextBlock x:Name="ErrorText" FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0"
                       Foreground="{DynamicResource WarnText}" Visibility="Collapsed"/>

            <Grid Margin="0,16,0,0">
                <Button x:Name="ClearButton" Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Left" Visibility="Collapsed">
                    <TextBlock Text="Clear" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                </Button>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0">
                        <TextBlock Text="Cancel" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                    </Button>
                    <Button x:Name="SaveButton" Style="{StaticResource PrimaryButton}">
                        <TextBlock Text="Save" FontSize="13" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</Window>
